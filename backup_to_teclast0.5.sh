#! /bin/bash

#this .sh script is undone

#command1 || {echo decrypting/mounting ; command2 ;}
#if list ; then list ; [else list ;] fi
#in 'a == b"c"' or 'a != b"c"', this is a pattern matching expressiong, b is pattern, c is fixed string

#echo -? <root time=\"$(/bin/date)\">\n</root>\n \  # Do > and < need to be escaped?
#  > '/mnt/g/backup_zm/backup log.xml'

